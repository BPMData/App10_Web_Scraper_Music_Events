CREATE TABLE cats (
    name VARCHAR(50),
    age INT
);
# This is a comment, I set it to go by pound sign rather than -- because IDK I'm used to it

CREATE TABLE dogs (
    name VARCHAR(50),
    breed VARCHAR(100),
    age INT
);

DROP TABLE cats;
# Cats drool dogs rule

CREATE TABLE treats(
    name VARCHAR(50),
    quantity INT
);

DROP TABLE treats;